import { FilterSearchedRecipePipe } from './filter-searched-recipe.pipe';

describe('FilterSearchedRecipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSearchedRecipePipe();
    expect(pipe).toBeTruthy();
  });
});
